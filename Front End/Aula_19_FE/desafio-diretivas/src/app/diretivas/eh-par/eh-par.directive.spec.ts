import { EhParDirective } from './eh-par.directive';

describe('EhParDirective', () => {
  it('should create an instance', () => {
    const directive = new EhParDirective();
    expect(directive).toBeTruthy();
  });
});
