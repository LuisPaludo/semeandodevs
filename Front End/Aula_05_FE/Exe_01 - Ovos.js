// Declaração das variáveis
var duzias = prompt("Digite quantas dúzias de ovos serão adquiridas");

// Transformação de string para int, com cálculo de multiplicação
var ovos = parseInt(duzias)*12;

// Impressão do resultado no console
console.log(ovos);